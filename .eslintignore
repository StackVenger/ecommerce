apps/api/api/
packages/database/prisma/
# Bare Tailwind config files in the shared UI package sit outside its
# tsconfig (src/ only), so the type-aware parser can't lint them; they
# are static preset/palette definitions.
packages/ui/tailwind.config.ts
packages/ui/tailwind.palette.ts
